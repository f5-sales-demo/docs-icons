---
import iconData from '@iconify-json/carbon/icons.json';
import BaseIcon from './BaseIcon.astro';

interface Props {
  name: string;
  size?: string;
  label?: string;
  class?: string;
}

const props = Astro.props;
---

<BaseIcon icons={iconData.icons} defaultWidth={32} defaultHeight={32} {...props} />
